# Origin of the Nasara Agro API. Leave EMPTY to use this app's own /api/v1
# route handlers (the stand-in office) — point it at the real backend when it
# ships and nothing else changes.
NEXT_PUBLIC_SERVER_URI=

# Canonical site origin for metadata/sitemap/OG (falls back to production).
NEXT_PUBLIC_BASE_URL=http://localhost:3000
